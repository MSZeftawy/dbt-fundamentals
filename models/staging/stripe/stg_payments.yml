version: 2

models:
  - name: stg_payments
    description: One unique transaction per row
    columns: 
        - name: payment_id
          description: Primary key for stg_payments
          tests:
            - unique
            - not_null
